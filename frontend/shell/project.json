{
  "name": "shell",
  "sourceRoot": "shell/src",
  "projectType": "application",
  "targets": {
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "piral debug",
        "cwd": "shell"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "piral build",
        "cwd": "shell"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "piral validate",
        "cwd": "shell"
      }
    }
  }
}
