# Azure AD Authentication Configuration
# Get these values from your Azure AD app registration

# Azure AD Application (client) ID
VITE_MSFT_CLIENT_ID=your-client-id-here

# Azure AD Authority URL
# For personal Microsoft accounts only: https://login.microsoftonline.com/consumers
VITE_MSFT_AUTHORITY=https://login.microsoftonline.com/consumers

# Redirect URI (must match Azure AD app registration)
VITE_MSFT_REDIRECT_URI=http://localhost:1234/auth

# Post logout redirect URI
VITE_MSFT_POST_LOGOUT_REDIRECT_URI=http://localhost:1234/logout

# API Configuration
# Base URL for backend API
VITE_API_BASE_URL=https://localhost:7226
