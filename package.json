{
  "name": "mfe-portal-workspace",
  "private": true,
  "description": "Root workspace tooling for mfe-portal monorepo",
  "scripts": {
    "openspec": "OPENSPEC_TELEMETRY=0 npx openspec"
  },
  "config": {
    "openspec": {
      "telemetry": false,
      "note": "Opt-out is enforced via OPENSPEC_TELEMETRY=0. Set this env var in your shell or .env.local before running npx openspec directly."
    }
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.2.0"
  }
}
