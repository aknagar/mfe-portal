{
  "name": "micro-frontend-workspace",
  "version": "1.0.0",
  "description": "Piral Micro-Frontend Workspace with Nx",
  "private": true,
  "scripts": {
    "start": "nx run shell:start",
    "build": "nx run shell:build",
    "start:url-getter": "nx run url-getter-pilet:start",
    "build:url-getter": "nx run url-getter-pilet:build",
    "start:hello-world": "nx run hello-world-pilet:start",
    "build:hello-world": "nx run hello-world-pilet:build",
    "build:all": "nx run-many -t build",
    "validate": "nx run shell:validate"
  },
  "devDependencies": {
    "@nx/js": "^20.4.3",
    "@nx/vite": "^20.4.3",
    "@playwright/test": "^1.57.0",
    "nx": "^20.4.3",
    "playwright": "^1.57.0"
  }
}
